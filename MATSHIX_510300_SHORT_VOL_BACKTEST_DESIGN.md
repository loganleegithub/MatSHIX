# MatSHIX 驱动的 510300 期权动态 Short-Vol 回测设计

> 状态：已执行分钟成交代理；择时能力改由独立机会日诊断优先评价<br>
> 日期：2026-08-21<br>
> 初始资金：人民币 1,000,000 元<br>
> 数据根：`/Users/logan/OptiMatrix_DATA/AETF`<br>
> 研究目的：先判断 MatSHIX 是否能把未来不适合卖波动率的场景放进禁做区，再把分钟成交、费用和仓位路径作为二级交易压力测试

## 1. 先冻结结论

回测数据期固定为 `2023-01-03` 至 `2026-06-05`。AETF 源目录含完整 ETF/期权分钟 OHLCV、成交额和 OI；此前 MatSHIX 工程只提取 14:56，并不表示源数据只有一根分钟线。

仍有两个不能跨越的事实边界：

- 分钟数据没有 bid/ask 和订单簿，无法证明某一张订单的精确可成交价；主结果必须叫 `MINUTE_EXECUTION_PROXY`，不能叫 tick 级正式成交回测。
- MatSHIX v1 需要历史暖机和四市场横截面。`daily_market_state` 的首个可发布 OK 状态是 2024-06-14，对应最早决策时间 2024-06-17 09:00、最早模拟成交窗口 2024-06-17 09:36–09:45。不得为了从 2023 年产生交易而放宽 `UNKNOWN/PARTIAL`。

### 1.1 目标调整：择时诊断优先，成交模拟其次

tick bid/ask 缺失意味着账户回放不可能 100% 还原，但这不阻止评价气象站的方向。主研究问题改为：

> 在每个当时可选择同规则 510300 铁鹰的机会日，MatSHIX 已知状态是否让未来坏的 Short-Vol 结果更集中于禁做区，并让允许区的结果更好？

主诊断固定为 `CAUSAL_STATE_HINDSIGHT_OUTCOME_DIAGNOSTIC`：

- 仍使用 t 时点之前已知的 MatSHIX 状态和冻结的 25–45 DTE、20-delta 铁鹰选择规则；
- 建仓日与未来观察日均用四腿官方日结算价标记，不计 bid/ask、滑点、费用、数量和复利；
- P&L 除以建仓定义最大损失，主结果窗持有至 7DTE，并报告 5/10 个交易日稳健性；
- 将 `dynamic_unit>0` 记为 `ALLOW`，完整状态下 `dynamic_unit=0` 记为 `KNOWN_BLOCK`；纯输入不完整记为 `ABSTAIN_DATA`，不算气象站成功避险；
- 核心指标是允许区减禁做区的平均结果、最差 10% 机会日捕获率、风险单位单调性，并按完整到期月份成簇 bootstrap，避免把重叠机会日当成独立交易；
- 另用未来 ETF 实现波动率 / t-1 ATM IV 做市场层复核，区分气象站状态问题与铁鹰结构、持有周期或策略映射问题。
- 再运行 1 组铁鹰、零成本、日结算价的理想化状态路径：静态控制组只在状态完整的机会日建仓；动态组分别测试 `KNOWN_BLOCK` 退出与 `KNOWN_BLOCK/ABSTAIN_DATA` 均退出。前者评价已知状态，后者只说明实际安全弃权效果，二者不得混为择时命中。

只有主诊断先显示方向成立，分钟成交代理才用于回答“这种方向能否转化为可交易账户结果”。分钟回测不能反过来替代择时能力检验。

结果分成两个面板：

| 面板 | 时间 | 用途 |
|---|---|---|
| A：2023 起完整账户路径 | 2023-01-03 至 2026-06-05 | ETF 首日建仓；静态 Short-Vol 可从首个合格周期开始；动态策略在暖机期严格持有现金，2024-06-17 后才可能建仓 |
| B：共同可比期 | 2024-06-17 至 2026-06-05 | 三条策略以 1,000,000 元重新起算，比较完整账户路径；不再单独承担气象站择时能力的归因结论 |

面板 A 与 B 不拼接。面板 A 如实包含动态策略的暖机现金期，面板 B 用于比较共同期账户路径；择时方向由 1.1 的独立机会日诊断判断。不得使用 CSI300 单市场代理或事后填补早期 MatSHIX 状态；若另做单市场研究，必须命名为 `CSI300_ONLY_RESEARCH`。

## 2. 三条净值，而不是只看两条

### 2.1 `ETF_BH`

- 2023-01-03 用 09:36–09:45 的 510300 ETF 分钟成交 VWAP 加 1 个最小价位，按 100 份整数手买入；余款留现金。
- 使用可审计的复权总回报价格；分红在实际可用日后的下一可交易时点再投资。
- 除公司行动、分红再投资和末日估值外不交易。
- 计入真实适用的经纪费和历史费率；不能取得历史费率时，同时报告零费率与保守费率情景。

### 2.2 `SV_STATIC`（归因控制组）

- 使用与动态策略完全相同的合约选择、风险上限、滚动、退出、报价和成本规则。
- 在数据与执行合格时固定使用 `risk_unit=0.50`，不读取 MatSHIX phase、分数或概率。
- 它不是最终产品，而是回答：收益来自一般波动率风险溢价，还是来自 MatSHIX 的时机选择。

### 2.3 `SV_MATSHIX`（主策略）

- 只读取已发布的 MatSHIX 快照和 CSI300 局部状态；不读取未来事件标签或未来概率结果。
- 将气象状态映射为目标风险单位，再操作 510300 期权。
- MatSHIX 仍然是市场状态引擎，不拥有仓位、下单权限或“策略赚钱概率”。

稳定边界为：

```text
AETF 分钟 OHLCV/amount/OI（无 bid/ask）
  -> MatSHIX DailySnapshot(t, decision_as_of=t+1 09:00)
  -> 510300 Strategy Adapter
  -> DesiredRiskUnit(t+1)
  -> 合约选择 / 组合数量 / 订单
  -> 独立交易与 PnL ledger
```

## 3. 主交易结构：定义风险铁鹰

每次只持有一个到期月份的一组 510300 铁鹰：

1. 只使用 `per_unit=10000`、`opt_multiplier=10000` 且 `symbol NOT LIKE '%A%'` 的标准合约。当前静态主表不能还原完整逐日调整链，调整合约一律拒绝，不用今天的条款倒推历史。
2. 选择 25–45 自然日到期、最接近 35 DTE 的到期月；若没有合格到期月则不交易。
3. 卖出绝对 Delta 最接近 0.20 的 OTM Put 和 OTM Call，允许范围为 0.15–0.25。Delta 使用 t-1 已知曲面与 t+1 09:35 已完成的 ETF 分钟价格计算，订单从 09:36 才开始，不能使用后续期权分钟价格反选合约。
4. 在两侧分别买入至少再远两个挂牌行权价档位的保护翼；四腿必须同到期月、同合约单位。
5. 每侧最大亏损均可由行权价差、净权利金和合约单位确定；组合出现负净权利金、缺腿或条款不一致时拒绝交易。
6. 组合建仓时绝对净 Delta 必须不高于 0.10；主结果不做盘中 Delta 对冲，Delta 对冲仅作为独立敏感性测试。
7. 最迟在 7 DTE 买回全部四腿，不进入到期行权和实物交割。

选择铁鹰而不是裸卖跨式/宽跨式的原因是：它仍然是明确的 Short-Vol/Short-Gamma 结构，但尾部最大损失可计算，100 万账户不会因为保证金模型和事后幸存而得到虚假的高复利。

上交所当前合约条款显示，510300 期权标准合约单位为 10,000 份、欧式行权、通常实物交割、最小报价单位 0.0001 元。官方条款：<https://www.sse.com.cn/assortment/options/contract/c/c_20230303_5717360.shtml>。

## 4. MatSHIX 到风险单位的冻结映射

### 4.1 硬准入门

任一条件不满足，目标风险单位直接为 0，已有仓位在下一可成交时点平仓：

- 只允许 `session_date=t` 的状态影响下一交易日 t+1；`decision_as_of <= 09:35`，订单窗口从 09:36 开始；
- 状态明确标记 `evidence_tier=RESEARCH_ONLY`、`vintage_kind=PROVIDER_RECONSTRUCTED` 和 `availability=UNPROVEN`，策略结果继承同一证据降级，不伪装成 PIT 正式发布；
- 顶层 `data_status=OK`、`primary_phase != UNKNOWN`；
- CSI300 局部 `data_status=OK`；
- CSI300 t-1 曲面、标准合约条款和 t+1 分钟成交窗口合格；
- `insurance_compensation` 不是 `THIN/UNKNOWN`，且 `vrp_ewma94 > 0`；
- revision、config、source manifest、input 和 engine artifact hash 完整；
- 没有停牌、条款冲突或持仓限制阻断。AETF 没有每日涨跌停参数；若分钟窗口呈现无成交或价格锁死，按流动性门拒单，而不是猜测可成交。

MatSHIX 当前条件概率尚未通过样本外发布门，`outlook_answer=BASE_RATE_ONLY/UNKNOWN` 不参与仓位映射。

### 4.2 Phase 基础风险单位

| `primary_phase` | 基础风险单位 | 交易解释 |
|---|---:|---|
| `CALM_POSITIVE_VRP` | 1.00 | 平静且预期波动率补偿为正，允许完整预算 |
| `BALANCED_MARKET` | 0.75 | 常态市场，保留但不过度放大 Short-Vol |
| `REPAIR_IN_PROGRESS` | 0.50 | 修复已由 MatSHIX 滞回确认，但仍处于压力后阶段 |
| `DOWNSIDE_TAIL_RICH` | 0.25 | 保险较贵，但下行尾部风险本身也高 |
| `UPSIDE_CONVEXITY_PRICED` | 0.25 | 上行凸性被重估，限制卖 Call 风险 |
| `FRAGMENTED_TRANSITION` | 0.25 | 横截面不一致，只保留观察仓 |
| `LOCAL_STYLE_PRESSURE` | 0.25 | 非大盘局部压力也可能扩散，只保留观察仓 |
| `BLUE_CHIP_PRESSURE` | 0.00 | 直接涉及 510300 所属大盘风险段 |
| `BROAD_PRESSURE` | 0.00 | 广泛压力，不卖波动率 |
| `BROAD_PERSISTENT_PRESSURE` | 0.00 | 持续广泛压力，不卖波动率 |
| `LOCALIZED_ACUTE_STRESS` | 0.00 | 急性重定价，不卖波动率 |
| `SYSTEMIC_ACUTE_STRESS` | 0.00 | 系统性急压，不卖波动率 |
| `UNKNOWN` 或其他未注册值 | 0.00 | 未知不是中性，持有现金 |

### 4.3 CSI300 局部上限

按顺序应用以下上限：

- `insurance_compensation=NORMAL`：风险单位最高 0.50；`RICH` 不额外限制。
- 顶层 `direction=RISING` 或 `pressure_score >= 70`：最高 0.25。
- CSI300 的 `index_pressure >= 70`、`shock >= 65`、`down_tail >= 75` 或 `persistence >= 75` 任一成立：最高 0.25。
- CSI300 的 `index_pressure >= 85`、`shock >= 85` 或 MatSHIX `hard_acute=true` 任一成立：降为 0。

这些阈值在第一次读取策略 PnL 前冻结。不得根据回测曲线修改；任何修改必须形成新的 policy version 和独立样本外结果。

## 5. 资金和数量

每个交易日先计算当前 NAV，再计算：

```text
defined_risk_budget = NAV * 8% * risk_unit
contracts_by_loss = floor(defined_risk_budget / max_loss_per_iron_condor)
target_contracts = min(contracts_by_loss, contracts_by_margin, contracts_by_liquidity)
```

冻结约束：

- 满风险单位下，单到期组合的定义最大损失不超过 NAV 的 8%；
- 经纪商压力保证金占用不超过 NAV 的 35%；以当时有效的交易所最低标准和经纪商加收标准中较高者为准；
- 单腿数量不超过该合约最近 20 个交易日同时间窗口中位成交量的 5%；
- 权利金收入不在同一周期内再次加杠杆；
- 最多持有一个到期月，不叠加多个周期；
- 8% 是预注册的主风险预算，另报告 5% 和 12% 敏感性，不择优挑选主结果。

未投入资金按 0% 收益计，作为冻结的保守口径；本版本不引入外部现金利率，以免新增不可验证的历史可用时间。

## 6. 决策、建仓和退出时间

### 6.1 无未来函数

- `session_date=t` 的 MatSHIX 快照最早只允许在下一上交所期权交易日 09:00 使用。
- 策略在 09:35 读取一次风险单位和当分钟已经结束的 ETF 价格，只允许用 09:36–09:45 的期权成交分钟执行。
- 信号下降立即减仓；风险单位上升须连续两个决策日成立后才加仓。
- 当日 14:56 气象站值不得用于当日早盘或盘中交易。

### 6.2 建仓与滚动

- 无仓位且目标数量大于 0时建仓。
- 到 7 DTE 时全部平仓；若目标风险仍大于 0，下一交易日重新选择 25–45 DTE 组合。
- 目标数量下降时按整数张减仓；目标数量上升按两日确认规则增加。
- `UNKNOWN`、任何硬准入失败或目标风险为 0 时全部平仓，随后持有现金。
- 单组铁鹰按当日结算价标记的累计亏损达到定义最大损失的 50% 时，下一交易日进入强制退出队列。没有 bid/ask 时不模拟无法验证的盘中止损；保护翼承担隔夜尾部上限。
- 不设基于回测最优值的止盈。主结果持有至风险退出、硬止损或 7 DTE。

## 7. 成交、成本和中国市场细节

### 7.1 AETF 可用字段与本地审计

分钟期权表含 `open/high/low/close/vol/amount/oi`。对标准合约且 `vol>0` 的分钟：

```text
print_vwap = sum(amount) / (sum(vol) * 10000)
```

本地数据审计在每个交易日选择 25–45 DTE、最接近 35 DTE 的 510300 标准合约到期月，并检查 09:36–09:45：

- 2023-01-16 至 2026-05-29 共 521 个存在目标到期月的窗口；
- 513 个窗口至少各有 2 个成交活跃的 OTM Call 和 Put，占 98.46%；
- 510 个窗口至少各有 4 个成交活跃的 OTM Call 和 Put，占 97.89%；
- 85,219 根正成交分钟线中，85,182 根的 amount/volume VWAP 落在 OHLC 加减一个 tick 内，占 99.9566%。

这些统计证明成交额/成交量可以作为分钟成交均价代理，不证明目标四腿和目标数量每天都能成交。最终仍按逐腿准入，不能用总体覆盖替代订单证据。

### 7.2 逐腿成交门

建仓四腿必须在同一个 09:36–09:45 窗口全部满足：

- 标准合约且静态条款在当日有效；
- 至少 2 根 `vol>0` 的分钟线；
- 窗口总成交量不少于 20 张；
- 订单数量 `q <= floor(window_volume * 5%)`；
- `print_vwap` 落在所有正成交分钟合并的 `[window_low-tick, window_high+tick]`；
- OHLC、amount、volume、OI 非负且内部一致。

任一腿失败则整组铁鹰不建仓，不允许用 `close`、前收、结算价或理论价替代实际成交窗口。四腿通过只代表十分钟内分别存在成交，不代表交易所原子组合成交，因此每笔记录 `basket_fill_approximation=true`。

### 7.3 三个冻结成交情景

令 `R = window_high - window_low`，所有价格按该合约 tick 向不利方向取整：

| 情景 | 卖出腿 | 买入腿 | 用途 |
|---|---|---|---|
| `VWAP_2TICK`（主结果） | `print_vwap - 2*tick` | `print_vwap + 2*tick` | 小于等于 5% 窗口参与率下的成交代理；2 tick 是未观测价差的明确假设 |
| `VWAP_RANGE_25` | `print_vwap - max(2*tick, 0.25*R)` | `print_vwap + max(2*tick, 0.25*R)` | 将部分十分钟价格波动视为腿差和冲击成本 |
| `BAR_EXTREME` | `window_low` | `window_high` | 极保守边界，不作为最可能成交价 |

主报告同时展示三条净值。策略结论若只在 `VWAP_2TICK` 成立、在 `VWAP_RANGE_25` 消失，应定性为成本敏感，而不是稳健有效。

### 7.4 平仓与无法成交

- 正常平仓先尝试次日 09:36–09:45；四腿规则与建仓一致，买回空头腿、卖出保护翼时分别使用对应不利方向价格。
- 若风险退出在早盘未通过成交门，当日 13:31–13:40 重试；仍失败则在 14:46–14:55 最后重试并使用 `BAR_EXTREME`。
- 三个窗口均失败则保留真实仓位和定义风险，记录 `EXIT_UNFILLED`，下一交易日继续退出；不得用结算价虚构平仓。
- 日终 NAV 优先用期权日线 `settle` 标记；缺失时使用 14:46–14:56 最后正成交 VWAP；再缺失才使用模型价并标记 `MODEL_MARK`。标记价永远不能成为成交价。

### 7.5 费用

逐腿、逐张、逐方向计费。官方公开口径显示 ETF 期权交易经手费为每张 1.3 元、结算费为每张 0.3 元，卖出开仓在试点期间存在暂免项；经纪商佣金不在 AETF 中。回测同时输出：

- `OFFICIAL_MINIMUM`：按每个历史日期适用的官方经手费和结算费；
- `BROKER_PLUS_2`：官方费用外每张每次再加 2 元经纪佣金，作为主净费口径；
- `BROKER_PLUS_5`：官方费用外每张每次再加 5 元压力情景。

2 元和 5 元不是已验证账户费率，只是透明的敏感性参数；取得实际券商对账单后替换主口径。官方收费起点：[上交所收费标准](https://www.sse.com.cn/lawandrules/sselawsrules2025/charge/c/c_20250610_10781461.shtml)、[中国结算收费表](https://www.chinaclear.cn/zdjs/fbzyls/202506/9d22b74d9f2e40edb67b44d1f6596f18/files/%E4%B8%8A%E6%B5%B7%E5%B8%82%E5%9C%BA%E8%AF%81%E5%88%B8%E7%99%BB%E8%AE%B0%E7%BB%93%E7%AE%97%E4%B8%9A%E5%8A%A1%E6%94%B6%E8%B4%B9%E5%8F%8A%E4%BB%A3%E6%94%B6%E7%A8%8E%E8%B4%B9%E4%B8%80%E8%A7%88%E8%A1%A8.pdf)。

上交所的组合策略规则承认垂直价差、空头跨式和空头宽跨式等组合并规定保证金处理，但经纪商实际组合保证金仍无法从 AETF 恢复。因此主仓位始终按定义最大损失预算，不把推测的保证金优惠转化为杠杆。官方指引：<https://www.sse.com.cn/lawandrules/sselawsrules2025/option/c/c_20250610_10781452.shtml>。

## 8. 数据分层与最早可回测日期

| 层 | 所需数据 | 允许结论 |
|---|---|---|
| ETF 分钟层 | `ETF/1m_etf/*/*.parquet`、日复权因子和费用 | 2023-01-03 起 510300 买入持有分钟代理结果 |
| 静态期权分钟层 | `OPTION/1m_opt/*/*.parquet`、日线结算、标准合约静态条款 | 2023-01-03 起静态铁鹰分钟成交代理结果 |
| MatSHIX 暖机层 | 2023-01-03 至 2024-06-14 的 `UNKNOWN/PARTIAL` 状态 | 动态策略持有现金；只报告为何不可交易 |
| MatSHIX 共同层 | 2024-06-17 起可发布状态、510300 分钟成交和 ETF 分钟数据 | `ETF_BH`、`SV_STATIC`、`SV_MATSHIX` 的共同期分钟代理比较 |

AETF 的共同截止日为 2026-06-05；最后一个可执行 MatSHIX 状态通常来自前一交易日，不能使用 2026-06-05 状态去交易并不存在的 2026-06-08 行情。

全套结果固定标记：

```text
execution_evidence = MINUTE_OHLCV_AMOUNT_PROXY
quote_evidence = BID_ASK_UNAVAILABLE
state_evidence = RESEARCH_ONLY_PROVIDER_RECONSTRUCTED
formal_tick_backtest = false
```

## 9. 结果表和归因

每条净值至少报告：

- 期末净值、累计收益、CAGR；
- 年化波动、Sharpe、Sortino、Calmar；
- 最大回撤、最长回撤修复天数；
- 最差日/周/月、95%/99% 历史 Expected Shortfall；
- 月度胜率、盈利因子、换手、交易次数和平均持有期；
- 平均/峰值定义风险、保证金占用、Delta、Gamma、Vega、Theta；
- 毛收益、分钟成交代理折价、费用、退出未成交次数；
- `UNKNOWN` 占比、空仓占比、每个 phase 的天数与 PnL；
- 对 5%/8%/12% 风险预算和成本压力带的敏感性；
- 使用交易日 block bootstrap 的 CAGR、最大回撤和策略差值置信区间。

归因顺序：

1. `SV_MATSHIX - SV_STATIC`：天气时机选择的增量；
2. `SV_STATIC - cash`：一般 Short-Vol 风险溢价；
3. `ETF_BH - cash`：股票 Beta 与分红；
4. Delta 对冲敏感性：Short-Vol 的结果有多少来自方向敞口。

不能因为 `SV_MATSHIX` 绝对收益高于 ETF 就声称气象站有效。至少还要看到它相对 `SV_STATIC` 的净费后风险调整改善，并且改善不依赖单一风险预算、单一成本假设或少数日期。

## 10. 防偏差和测试合同

分钟代理运行前必须通过：

1. **时间测试**：t 日 14:56 状态只能影响 t+1，合约选择最多使用到 09:35，期权成交只能来自 09:36 以后。
2. **证据降级测试**：`availability=UNPROVEN` 必须贯穿结果，任何分钟代理产物都不能标为正式 PIT/tick 回测。
3. **UNKNOWN 测试**：缺失、PARTIAL、UNKNOWN、未注册 phase 均得到 0 风险单位。
4. **合约测试**：只准入标准 10,000 单位合约；调整合约、过期、未上市和条款冲突全部拒绝。
5. **成交均价测试**：标准合约 `amount/(vol*10000)` 必须落入 OHLC 加减一 tick；否则该腿拒绝。
6. **流动性测试**：四腿分别满足正成交分钟数、最小窗口量和不高于 5% 参与率；缺腿时整组不得建仓。
7. **成交方向测试**：卖出向下、买入向上取整；三个成交情景均不得优于原始 VWAP。
8. **退出未成交测试**：失败退出保留仓位并重试，禁止用结算价伪造平仓。
9. **最大损失测试**：任意铁鹰的最坏情形损失不超过冻结预算。
10. **现金与 NAV 对账**：每日 `cash + positions_mark = NAV`，权利金、费用和标记变动逐笔闭合。
11. **不可变重放**：相同 revision/config/input/engine/policy hash 得到相同订单和 PnL hash。
12. **控制组同构**：静态与动态策略除风险单位来源外完全相同。
13. **覆盖报告**：按年份、phase、到期月、成交情景报告信号、流动性和拒单率。

## 11. 验收门

只有同时满足以下条件才发布 `MINUTE_EXECUTION_PROXY` 比较：

- 策略 policy 在读取 PnL 前已冻结并内容寻址；
- AETF 2023-01-03 至 2026-06-05 的 ETF/期权分钟文件、日结算和标准合约静态条款通过验收；
- 明确接受 MatSHIX `RESEARCH_ONLY + PROVIDER_RECONSTRUCTED` 和分钟无 bid/ask 的证据上限；
- 三条策略在相同共同日期、相同初始资金下运行；
- 单元测试、类型检查、lint、确定性重放和逐日会计对账通过；
- 同时交付机器可读交易 ledger、每日 NAV、拒单 ledger、指标 JSON 和图表；
- 报告清楚分开“2023 起完整账户路径”和“2024-06-17 起共同期比较”，并同时展示三种成交情景与费用情景。

该验收可以支持一个诚实、尽量贴近真实成交的分钟代理回测，但不能证明真实账户会获得同样成交价，也不能升级为 tick bid/ask 回测。
